package com.baypal;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

// the simplest possible test: if the whole app context can start up without
// throwing (all beans wire together correctly), this passes.
@SpringBootTest
class BayPalApplicationTests {

    @Test
    void contextLoads() {
    }
}
